# AppArmor profile for daemonset-vizier-pem
profile daemonset-vizier-pem-profile flags=(attach_disconnected,mediate_deleted) {
  #include <tunables/global>
  # Executable: /app/src/vizier/services/agent/pem/pem
  /app/src/vizier/services/agent/pem/pem \{
    capability BPF,
    capability DAC_OVERRIDE,
    capability DAC_READ_SEARCH,
    capability IPC_LOCK,
    capability NET_ADMIN,
    capability PERFMON,
    capability SYS_ADMIN,
    capability SYS_PTRACE,
    capability SYSLOG,
  }
}
