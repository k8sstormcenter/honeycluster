# AppArmor profile for replicaset-olm-operator
profile replicaset-olm-operator-profile flags=(attach_disconnected,mediate_deleted) {
  #include <tunables/global>
  # Executable: /bin/olm
  /bin/olm \{
    capability DAC_OVERRIDE,
    capability DAC_READ_SEARCH,
    capability NET_ADMIN,
  }
}
