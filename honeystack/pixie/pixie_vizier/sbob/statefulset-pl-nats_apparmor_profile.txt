# AppArmor profile for statefulset-pl-nats
profile statefulset-pl-nats-profile flags=(attach_disconnected,mediate_deleted) {
  #include <tunables/global>
  # Executable: /nats-server
  /nats-server \{
    capability DAC_OVERRIDE,
    capability DAC_READ_SEARCH,
    capability NET_ADMIN,
  }
}
