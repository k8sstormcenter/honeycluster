# AppArmor profile for job
profile job-profile flags=(attach_disconnected,mediate_deleted) {
  #include <tunables/global>
  # Executable: /bin/opm
  /bin/opm \{
    capability DAC_OVERRIDE,
    capability DAC_READ_SEARCH,
  }
}
